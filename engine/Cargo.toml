[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
chess-core = { path = "../chess-core" }
indicatif = { version = "0.18.4", optional = true }
rand = "0.10.2"

[features]
progress = ["indicatif"]
tt-stats = []