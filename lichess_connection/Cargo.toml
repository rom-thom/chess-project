[package]
name = "ui"
version = "0.1.0"
edition = "2024"

[dependencies]
chess-core = { path = "../chess-core" }
engine = { path = "../engine"}

dotenv = "0.15.0"
reqwest = { version = "0.12.22", features = ["json", "blocking"] }